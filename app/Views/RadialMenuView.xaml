<UserControl x:Class="CodexController.Views.RadialMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:CodexController.Views"
             mc:Ignorable="d"
             d:DesignHeight="560"
             d:DesignWidth="980"
             IsHitTestVisible="False"
             SnapsToDevicePixels="True">
    <Grid Width="980"
          Height="560"
          Background="Transparent">
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Visibility" Value="Visible" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsVisible}" Value="False">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>

        <views:ActionMenuView Margin="0,12,0,0"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Top">
            <views:ActionMenuView.Style>
                <Style TargetType="views:ActionMenuView">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAgentLayer}"
                                     Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </views:ActionMenuView.Style>
        </views:ActionMenuView>

        <views:AgentKeypadView Margin="0,12,0,0"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Top">
            <views:AgentKeypadView.Style>
                <Style TargetType="views:AgentKeypadView">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAgentLayer}"
                                     Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </views:AgentKeypadView.Style>
        </views:AgentKeypadView>
    </Grid>
</UserControl>
